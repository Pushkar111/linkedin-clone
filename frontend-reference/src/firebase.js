/**
 * Firebase configuration removed - Application now uses REST API backend
 * This file is kept for backward compatibility but exports stub objects to prevent import errors
 */

// Firebase has been replaced with REST API backend
// See authService.js, userService.js, and postService.js for API services

// Export stub objects to prevent import errors
export const appDB = null;
export const appStorage = null;
export const appAuthProvider = null;
